
<% if @friends.size > 0 %>
  <ul>
    <% @friends.each do |friend| %>
      <li><%= "#{friend['firstName']}  #{friend['lastName']}" %></li>
      <p>Home City: <%= "#{friend['homeCity']}" %></p>
    <% end %>
  </ul>
<% else %>
    <strong>Looks like you don't have any foursquare friends.</strong>
<% end %>
